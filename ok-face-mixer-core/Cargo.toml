[package]
name = "ok-face-mixer-core"
version = "0.1.0"
edition = "2024"

[dependencies]
image = { version = "0.25.6", optional = true }
lazy_static = { version = "1.5.0", optional = true }
log = "0.4.27"

[features]
query = []
generator = ["dep:image", "dep:lazy_static"]
